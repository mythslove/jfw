package examples.animation.geom.isolib.map.scene 
{
	import examples.animation.geom.isolib.map.layers.ContainerLayer;

	public class Maplayer extends ContainerLayer
	{
		public function Maplayer(name:String=null)
		{
			super(name);
		}
	}
}